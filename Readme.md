This project is to showcase that rollup may not consider treeshaking in some cases.

Run the below command to generate the bundle inside dist folder.
    
 ```
 npm install
 
 npm run generate
 
 ```
    